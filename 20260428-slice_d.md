# 20260428-slice_d.md
## Slice D — Durable Application Surface

**Date:** 2026-04-28
**Status:** Proposed
**Depends on:** Slice C closed and verified (SYSTEM_INVENTORY.md 2026-04-28 06:04)

---

## Why This Group Exists Here

Group C proved the canonical turn engine works through validated Slice C runtime and service surfaces. Voice and text share one engine, sessions produce artifacts, interruption is deterministically tested, and the proving host (`scripts/run_jarvis.py`) makes failures visible. What does not yet exist is a durable application surface — a native desktop host that a user can launch, interact with continuously, and rely on across turns.

Group D builds that surface exactly once. The shell consumes the backend through an explicit API contract established in D.1. The Tauri native desktop application lands in D.2. Continuous resident interaction arrives in D.3. Wake-word invocation integrates into the resident loop in D.4. Personality and presence polish close the group in D.5.

The ordering is deliberate:
- D.1 fixes the backend-to-shell contract before any Tauri code is written, so the shell cannot accidentally own orchestration logic.
- D.2 builds the shell once against that fixed contract, on both host classes simultaneously.
- D.3 and D.4 extend the resident path incrementally rather than redesigning it.
- D.5 polishes without touching cognition or adding agents or tools.

After Group D, the application is usable as a continuous voice assistant on both supported Windows host classes. Everything in Groups E–I is capability extension on top of a working durable product.

---

## Group Goal

A user on either Windows x64 or Windows ARM64 launches JARVISv7 as a native desktop application, speaks naturally across multiple turns without manual re-invocation, invokes the assistant by wake word, sees hardware and runtime selection clearly, and experiences a consistent personality across turns. The backend remains authoritative. The desktop shell is a thin adapter.

---

## Slice C Baseline Consumed

All of the following are verified in `SYSTEM_INVENTORY.md` (2026-04-28) and consumed unchanged by Group D:

- `TurnEngine` — shared voice/text path with deterministic state machine; text and voice turns share one engine
- `TurnResult` — turn outcome including transcript, response, final state, failure reason, TTS degraded fields, interruption events
- `SessionManager` — session lifecycle, artifact recording, bounded working memory injection
- `TurnArtifact` / `SessionArtifact` — schema fixed in C.3; persisted to `data/turns/` and `data/sessions/`
- `WorkingMemory` / `WritePolicy` — bounded in-session context only; no cross-session or episodic retrieval
- `BargeInDetector` — RMS/guard-time interruption detection; deterministically tested only (no full acoustic mic barge-in claimed)
- `playback.play()` / `playback.stop()` — sole audio output entry points; TTS synthesis validated in C.2 but physical multi-turn audio-output/device-state validation not claimed
- `turn_service.run_text_turn()` / `turn_service.run_voice_turn()` — thin service boundary
- `voice_service.capture_audio()` — audio input entry point (single-turn; resident loop extension is D.3/D.4 scope)
- `PersonalityProfile` / `load_default_personality()` — explicit personality input contract; `adapter.py` is a stub at C closeout
- `select_stt_runtime()` / `select_tts_runtime()` / `select_llm()` / `wake_runtime.py` — hardware-evidence-driven selectors; no durable API/resident surface or routing-policy implementation exists yet
- `scripts/run_jarvis.py` — diagnostic proving host only; explicitly not the durable application surface
- Regression baseline: 74 tests green on Windows x64 and Windows ARM64

C.5 caveats preserved: no physical audio-output validation, no full acoustic microphone barge-in, no durable desktop/API/resident shell, no episodic memory, tools, agents, or routing/policy implementation claimed by Slice C.

No Group C files are modified by Group D unless a concrete conflict is found and documented.

---

## Out of Scope

Group D introduces no Group E/F/G/H/I implementation scope. Specific exclusions:

- No Redis, SearXNG, or cache layer (Group E)
- No tool registry, tool invocations, or ACTING state extension (Group F)
- No episodic memory, cross-session retrieval, or memory-retrieval injection (Group G)
- No local llama.cpp activation (H.1) — LLM runtime remains Ollama-primary
- No QNN STT inference (H.2) — QNN remains definition-only per A.6
- No DirectML/ROCm uplift (H.3)
- No agent framework, agent roles, or agent ledger (Group I)
- No new runtime families or hardware-selection logic — all runtime selection stays in Slice A/B boundaries
- No changes to `TurnArtifact` schema fields — additions allowed only if an optional field with a default value is strictly required; no renames or removals
- No changes to cognition, prompt assembly, or responder logic beyond what D.5 personality polish requires
- No production-grade authentication, multi-user, or cloud deployment scope
- `scripts/run_jarvis.py` remains the diagnostic proving host and is explicitly not promoted to a shipping surface

---

## Target Host Classes

- **Windows x64** (primary; NVIDIA GPU + CUDA common; proven through Slices A–C)
- **Windows ARM64** (primary; Qualcomm NPU; proven through Slices A–C)
- Linux is best-effort where Tauri supports it; not blocking for Group D closeout

---

## Method-Viability Assumptions

Gates are split by sub-slice. Agent verifies gates for the relevant sub-slice before beginning that sub-slice; later gates do not block earlier work.

### Pre-D.1 Gates

**Gate D-G4 — FastAPI and Uvicorn in base dependencies.**
`fastapi>=0.110` and `uvicorn[standard]>=0.27` are already declared in `pyproject.toml` base `[project].dependencies`. Confirm they are installed in the current provisioned environment. No new pip dependencies should be required for D.1.

**Gate D-G5 — `scripts/run_backend.py` exists or must be created.**
AGENTS.md §7 references `scripts/run_backend.py` as the “start backend API only” entry point. Confirm whether it exists; if not, it is new scope for D.1.

### Pre-D.2 Gates

**Gate D-G1 — Tauri 2.x Windows ARM64 toolchain.**
Per Tauri’s official documentation, building for ARM64 on Windows requires MSVC v143 — VS 2022 C++ ARM64 build tools and `rustup target add aarch64-pc-windows-msvc`. Confirm these are installed and Tauri 2.x produces a native ARM64 binary on the ARM64 dev host before D.2 begins. Document the confirmed toolchain version in `desktop/README.md`.

**Gate D-G2 — WebView2 runtime availability.**
WebView2 is present on Windows 11 by default. On Windows 10, the Evergreen Bootstrapper must be installed separately. Confirm both dev hosts have a working WebView2 runtime and identify the minimum required version. Document in `desktop/README.md`.

**Gate D-G3 — Tauri transport and D.2 audio ownership.**
Chosen transport: `scripts/run_backend.py` starts the FastAPI backend as a child process; the Tauri shell calls it over HTTP on localhost using the D.1 contract. Verify Uvicorn binding is compatible before writing Tauri code.

D.2 audio ownership is decided here, not left open:
- **Default PTT strategy:** Shell captures microphone audio via browser `getUserMedia`, encodes to WAV, and POSTs to `POST /task/voice`. Backend decodes, runs the turn, returns the response. If audio bytes are in the response the shell plays them via Web Audio API; if TTS is degraded, text only is displayed.
- This is the single D.2 audio path. If it proves incompatible during implementation, stop and surface a concrete blocker. Do not silently adopt an alternative.

### Pre-D.4 Gates

**Gate D-G6 — openWakeWord live microphone strategy.**
Confirm `openwakeword_runtime.detect()` processes live microphone chunks without blocking the resident loop. Decide how microphone contention between wake monitoring and per-turn capture is resolved: single shared `sounddevice` stream, or sequential open/close. Document the chosen strategy in `session_service.py` before D.4 implementation begins.

---

## New Directories and Files Created by This Slice

Uses existing `repo_tree.md` locations. No new top-level directories.

```
backend/app/api/                    ← new domain (D.1)
backend/app/api/routes/
backend/app/api/schemas/
backend/app/api/dependencies.py
backend/app/services/session_service.py   ← new in D.3
desktop/src/                        ← populated (D.2; src/ exists with .gitkeep only)
desktop/src-tauri/                  ← populated (D.2; src-tauri/ exists with .gitkeep only)
backend/tests/unit/api/
backend/tests/integration/api/
backend/tests/runtime/desktop/
```

`scripts/run_backend.py` — introduced in D.1 if not already present.

---

## Execution Order

```
Pre-D.1 gates (D-G4, D-G5) verified → D.1 → Pre-D.2 gates (D-G1, D-G2, D-G3) verified → D.2 → D.3 → Pre-D.4 gate (D-G6) verified → D.4 → D.5
```

D.1 must be complete and headless-client proven before D.2 Tauri code is written. Each sub-slice must compile and pass its unit suite before the next begins. Slice A+B+C regression (74 tests) must remain green after every sub-slice.

---

## Sub-Slice D.1 — Application Shell Contract

### Why Here

Before a single line of Tauri code is written, the backend must expose an explicit, typed HTTP contract. If the contract is defined after the shell is built, the shell will inevitably own logic that belongs in the backend.

### Goal

A headless HTTP client (curl, httpx, or a test fixture) drives the full application lifecycle — startup, session management, text and voice turns, diagnostics — without any shell involvement. The shell in D.2 consumes this contract and nothing else.

### Scope

**`scripts/run_backend.py`** — if not already present: starts the FastAPI application via Uvicorn on a configurable host/port. Accepts `--host`, `--port`, `--reload` (dev mode). Emits the host-fingerprint line as the first stdout line per AGENTS.md §7. Calls the profiler, preflight, and runtime selectors at startup and injects them into the FastAPI application state.

**`backend/app/api/dependencies.py`** — FastAPI dependency providers:
- `get_engine() -> TurnEngine` — returns the application-level `TurnEngine` instance.
- `get_session_manager() -> SessionManager` — returns the application-level `SessionManager`.
- `get_personality() -> PersonalityProfile` — returns the active personality.
- `get_preflight() -> PreflightResult` — returns the cached preflight result.
- `get_profile() -> HardwareProfile` — returns the hardware profile.

**`backend/app/api/schemas/`** — Pydantic request/response models:
- `common.py` — `ErrorResponse`, `HealthResponse`
- `readiness.py` — `ReadinessResponse`: per-family runtime + device + model + ready + reason; active personality profile id; active LLM runtime name; degraded mode indicators
- `session.py` — `CreateSessionRequest`, `CreateSessionResponse`, `CloseSessionRequest`, `CloseSessionResponse`
- `task.py` — `TextTurnRequest`, `TextTurnResponse` (transcript, response_text, final_state, failure_reason, turn_id, session_id)
- `voice.py` — `VoiceTurnResponse` (same fields as text plus tts_degraded, interrupted)

**`backend/app/api/routes/`** — one file per route group:
- `health.py` — `GET /health` → `HealthResponse`. Always returns 200 if backend is running.
- `readiness.py` — `GET /readiness` → `ReadinessResponse`. Emits the structured startup summary consumed by the shell.
- `session.py` — `POST /session/create` → `CreateSessionResponse`; `POST /session/close` → `CloseSessionResponse`.
- `task.py` — `POST /task/text` → `TextTurnResponse`. Accepts `TextTurnRequest`, runs `turn_service.run_text_turn`.
- `voice.py` — `POST /task/voice` with multipart audio upload → `VoiceTurnResponse`. Accepts raw WAV bytes, decodes to numpy, runs `turn_service.run_voice_turn`.
- `diagnostics.py` — `GET /diagnostics/profile` → hardware profile JSON; `GET /diagnostics/preflight` → preflight token list and probe errors.

- `agents.py` — `GET /agents/status` → `{enabled: false, reason: "Group I not implemented"}`. Required by `slices.md`; returns a consistent disabled response in D.1 so D.2 shell and later can call it without a 404.
- `status.py` — `GET /status/wake` → `{provider: str, available: bool, reason: str}`. Defining this in D.1 avoids D.4 introducing an ad hoc endpoint outside the declared contract.
- Voice route receives audio as bytes over HTTP, not a live microphone connection. Live microphone capture remains in `voice_service.capture_audio()` used by the resident loop (D.3). The shell POSTs captured audio to this endpoint.

**`backend/tests/unit/api/__init__.py`** — empty.

**`backend/tests/unit/api/test_routes.py`** — unit tests using FastAPI `TestClient`:
- `test_health_returns_200`
- `test_readiness_returns_per_family_readiness` (mock preflight + profile)
- `test_text_turn_returns_turn_result` (mock engine)
- `test_voice_turn_accepts_wav_bytes_and_returns_result` (mock engine)
- `test_session_create_returns_session_id` (mock session_manager)
- `test_session_close_returns_ok` (mock session_manager)
- `test_diagnostics_profile_returns_arch` (mock profile)

**`backend/tests/integration/api/__init__.py`** — empty.

**`backend/tests/integration/api/test_headless_client.py`** — `@pytest.mark.integration`:
- `test_headless_client_can_call_health_endpoint`
- `test_headless_client_can_call_readiness_endpoint`
- `test_headless_client_can_drive_text_turn` (mock LLM)

### Files Not to Modify

- `backend/app/conversation/**`, `backend/app/services/turn_service.py`, `backend/app/services/voice_service.py` — consumed, not modified
- `backend/app/runtimes/**`, `backend/app/hardware/**`
- All Slice A/B/C test files

### Key Design Decisions

- API schemas are declared once in `backend/app/api/schemas/` and treated as a compatibility boundary with the desktop shell from D.2 onward. Later slices may add optional response fields with defaults; they must not rename or remove existing fields.
- Session lifecycle is explicit: `POST /session/create` before any turns; `POST /session/close` after. The shell does not manage session state internally.
- Voice audio upload over HTTP is intentionally synchronous in D.1. Streaming WebSocket voice is future scope (not in any current group).
- Backend owns all orchestration; no orchestration logic in route handlers. Handlers call services and return responses.

### Validation Commands

```powershell
backend/.venv/Scripts/python -m compileall backend/app/api scripts/run_backend.py
backend/.venv/Scripts/python -m pytest backend/tests/unit/api -q
backend/.venv/Scripts/python -m pytest backend/tests/integration/api -q
backend/.venv/Scripts/python scripts/validate_backend.py unit
backend/.venv/Scripts/python scripts/validate_backend.py regression
```

### Closeout Evidence

```
PASS compile: backend/app/api, scripts/run_backend.py
PASS unit: api route unit suite — N passed on both host classes
PASS integration: headless client integration suite — N passed
PASS regression: validate_backend.py regression green on both host classes (count ≥ 74)
PASS live: scripts/run_backend.py starts; curl /health returns 200 on both host classes
```

### Finish Line

The D.1 contract is fixed and proven headless. D.2 Tauri code may begin.

### Risks

- FastAPI `TestClient` requires `httpx` (already in base dependencies). Confirm `httpx` version compatibility before writing tests.
- Voice endpoint: decoding raw WAV bytes to numpy may use stdlib `wave` or existing audio dependencies. Use the narrowest cross-host-safe path; do not mandate `soundfile` if stdlib `wave` is sufficient.

---

## Sub-Slice D.2 — Durable Desktop Host

### Why Here

The shell lands once on the durable surface, consuming D.1's fixed HTTP contract and the runner-proven toolchain. The proving host (`scripts/run_jarvis.py`) is explicitly demoted to developer/diagnostic scaffold at this slice's landing. `desktop/` is the durable surface from this point onward.

`scripts/dev_runner.py check --arch x64` and `check --arch arm64` must have exited 0 (or produced documented `FAIL` evidence with resolution) before D.2 implementation begins. That evidence is the Pre-D.2 gate proof.

### Goal

A user on either Windows host class launches JARVISv7 as a native desktop application, sees hardware and runtime selection in the tray/UI, and completes a text turn with PTT proof-of-path. Both host classes build natively without cross-compilation as the primary path.

### Dev/Build Prerequisites by Host Architecture

Documented in `README.md` and `desktop/README.md`. Record toolchain versions confirmed by `dev_runner.py check` evidence.

**Windows x64 developer host:**
- Rust stable toolchain via [rustup.rs](https://rustup.rs/)
- Node.js LTS (x64) + npm
- MSVC v143 build tools — “Desktop development with C++” workload in Visual Studio 2022 or Build Tools
- WebView2 runtime (Windows 11 default; install Evergreen Bootstrapper on Windows 10)

**Windows ARM64 developer host:**
- Rust stable toolchain via rustup + `rustup target add aarch64-pc-windows-msvc`
- Node.js LTS (ARM64 build preferred; x64 runs via emulation) + npm
- **VS 2022 (any edition) C++ ARM64 build tools**: in Visual Studio Installer select “MSVC v143 - VS 2022 C++ ARM64 build tools (Latest)”. Community, Professional, and BuildTools editions all work; the edition discovered by `vswhere` on the ARM64 host is valid.
- WebView2 runtime (Windows 11 ARM64 default)

**Cross-compile x64 → ARM64** (documented escape hatch; not the primary D.2 path):
```
backend/.venv/Scripts/python scripts/dev_runner.py check --arch x64-arm64
pnpm tauri build --target aarch64-pc-windows-msvc --bundles nsis
```
The NSIS installer runs via emulation; the bundled binary is native ARM64.

### Rust/Tauri Layer Ownership

The Rust/Tauri layer owns:
- Backend child process lifecycle (spawn, health-wait, kill)
- Startup failure diagnosis and surfacing to the UI
- Tauri command definitions consumed by the frontend JS

The frontend JS must **not** manage backend process state directly. It calls Tauri commands; the Rust layer owns the result.

### Scope

**`desktop/src-tauri/`** — Tauri 2.x Rust application:

- **`backend.rs`** — `BackendProcessManager` struct and impl:
  - `spawn_backend(port: u16) -> Result<(), String>`: resolves repo root from `CARGO_MANIFEST_DIR` (no `canonicalize()` — avoids `\\?\` prefix that breaks Python imports on Windows); constructs exact `backend/.venv/Scripts/python.exe` path and `scripts/run_backend.py` path; confirms both exist before spawning; sets `CREATE_NO_WINDOW` on Windows; redirects stdout/stderr to `reports/backend_spawn_stderr.log` and `reports/backend_startup.log` respectively; sleeps 350 ms then calls `try_wait()` to detect immediate exit with log tail included in error.
  - `wait_healthy(url, timeout_ms) -> Result<(), String>`: polls `GET /health` with short per-request timeout; checks `try_wait()` before each probe to detect early exit; returns log tails on failure or timeout.
  - `kill_backend()`, `is_running()`, `port()` accessors.
  - `DesktopState { backend: Arc<Mutex<BackendProcessManager>> }` — managed Tauri state.
  - Rust layer must **not** hold the `Arc<Mutex<...>>` lock while blocking on HTTP. Acquire lock, check/spawn, release lock, then poll health without holding the lock.

- **`lib.rs`** — Tauri command definitions and app setup:
  - `start_backend` command: acquires lock → kills any existing process → spawns → releases lock → polls health without holding lock → on healthy: calls `POST /session/create` → returns success string or detailed error.
  - `stop_backend` command: acquires lock → kills → releases.
  - `health_check` command: non-blocking single probe; returns `{"status":"ok"}`, `{"status":"starting"}`, or `{"status":"error","error":"..."}` without holding the lock during HTTP.
  - `get_readiness` command: `GET /readiness` → returns JSON string to JS.
  - `submit_text(text: String)` command: `POST /task/text` with JSON body → returns response JSON string.
  - `push_to_talk` command: captures audio via `getUserMedia` in the frontend, encodes to WAV, then invokes `submit_voice(audio_bytes)` Tauri command which POSTs to `POST /task/voice`. See PTT Note below.
  - PTT hotkey registration (e.g. Ctrl+Alt+J via `tauri-plugin-global-shortcut`).
  - `setup` hook: calls `tray::setup_tray`; registers hotkey; does **not** auto-start backend (startup is frontend-driven via `start_backend` command so the UI is visible before the blocking health-wait).
  - `on_window_event CloseRequested`: calls `stop_backend`.
  - `Cargo.toml`: `tauri 2.x`, `reqwest` with `blocking` feature, `tauri-plugin-global-shortcut 2.x`.
  - `tauri.conf.json`: app identifier, window config, tray config; allowed origins localhost only.
  - `build.rs`: standard Tauri build script.
  - `main.rs`: calls `jarvisv7_lib::run()`.

- **`tray.rs`** — system tray setup and state icon/label management:
  - Named tray states matching canonical `ConversationState`: Starting, Ready, Listening, Transcribing, Thinking, Speaking, Interrupted, Degraded, Failed.
  - Tray context menu: Start Backend, Stop Backend, Show Window, Quit.

**`desktop/src/`** — web UI (vanilla HTML/CSS/JS; keep thin; no framework required):

- **`index.html`** — shell; includes `main.js` and `style.css`.

- **`main.js`** — frontend logic:
  - On load: invoke `start_backend` Tauri command → poll `health_check` command until `status: ok` or error → on healthy: invoke `get_readiness` and populate hardware/runtime summary panel.
  - Backend startup failure: display the error string from `start_backend` prominently; do not silently hide it.
- **PTT button**: on press → start `getUserMedia` capture; on release → encode audio to WAV → invoke `submit_voice` Tauri command (which calls `POST /task/voice`) → on response → append to conversation display.
  - Text input: on submit → invoke `submit_text` command → append to conversation display.
  - Tray/state display: updated from turn response `final_state` field.
  - Uses `window.__TAURI__.core.invoke(...)` (Tauri 2.x API); does **not** use raw `fetch()` to the backend for lifecycle operations. Direct `fetch()` to the backend may be used for supplementary read-only calls (e.g. `/diagnostics/profile`) but not for session or turn operations.

- **`style.css`** — dark, high-contrast terminal/dashboard theme:
  - Background: near-black (e.g. `#0d0d0d` or `#111`).
  - Foreground: bright white or light cyan (e.g. `rgba(255,255,255,0.87)` or `#e0e0e0`).
  - Accent: a single highlight color for active states and borders (e.g. electric blue `#00bfff` or green `#00ff99`).
  - Monospace font for conversation transcript and status lines.
  - Proportional system font for labels and controls.
  - Minimal border-radius; flat controls; no shadows or gradients.
  - Conversation panel: scrollable; newest entry at bottom; timestamp per entry in muted color.
  - State indicator: always visible; uses accent color when active.

- **App icon**: use the Windows robot emoji 🤖 as the initial app icon placeholder (PNG rendered from the emoji for `icons/` folder). Full icon set can be polished in D.5.

- **`components/`**:
  - `status/` — state indicator; maps canonical states to label + accent color.
  - `hardware-selection/` — read-only panel; data from `/readiness` on startup.
  - `conversation/` — scrollable transcript log; each entry: timestamp, role, text.
  - `llm-selection/` — **read-only** display of active LLM runtime from `/readiness`. No interactive switching in D.2.

**`desktop/README.md`** — extend with arch-split prerequisites and the exact VS edition(s) confirmed by `dev_runner.py check` evidence on each host.

### PTT Note

D.2 PTT proof-of-path uses text turns first (invoke `submit_text`), then PTT voice if audio capture is viable. `getUserMedia` audio capture requires the WebView page to be served from localhost; confirm this works in the Tauri webview before making PTT the primary acceptance path. If `getUserMedia` does not function in the D.2 WebView, record the blocker and accept D.2 with text-turn-only proof; PTT audio is then D.3 scope.

### Files Not to Modify

- `backend/app/**` — backend consumed over HTTP, not modified
- `scripts/run_jarvis.py` — proving host unchanged
- `scripts/run_backend.py` — backend entry point unchanged
- `scripts/dev_runner.py` — runner unchanged

### Key Design Decisions

- Rust layer owns backend process lifecycle; frontend invokes Tauri commands only.
- `Arc<Mutex<BackendProcessManager>>` lock is never held during blocking HTTP calls.
- Backend subprocess uses `CREATE_NO_WINDOW` on Windows; stdout/stderr redirected to `reports/` log files.
- Repo root is resolved from `CARGO_MANIFEST_DIR` without `canonicalize()` to avoid the `\\?\` path prefix that breaks Python on Windows.
- Shell calls `POST /session/create` after health is confirmed, before the first turn.
- Session lifecycle in D.2 is per-launch (create on start, close on exit); resident multi-turn loop is D.3 scope.
- LLM selection is read-only in D.2.
- No WebSockets, no audio streaming, no resident wake loop in D.2.
- npm is the required package manager for D.2 (pnpm is optional; replace `pnpm` with `npm run` if pnpm is absent).

### Acceptance

- `scripts/dev_runner.py check --arch x64` exits 0 on x64 host (evidence in CHANGE_LOG).
- `scripts/dev_runner.py check --arch arm64` exits 0 on ARM64 host (evidence in CHANGE_LOG).
- Native Tauri dev build succeeds on Windows x64: `npm run tauri dev` produces a running window.
- Native Tauri dev build succeeds on Windows ARM64 using confirmed ARM64 toolchain.
- Desktop live on both host classes: launch → backend starts → readiness panel populates → text turn completes and appears in conversation display → tray reflects state transitions.
- Backend startup failure (e.g. Uvicorn not installed) is surfaced as a visible error in the UI, not a silent hang.
- Regression: validate_backend.py regression green on both host classes (≥ 74).

### Finish Line

A user on either Windows host class launches JARVISv7, sees it start the backend, completes at least one text turn in the conversation display, and sees hardware selection populated from `/readiness`. `desktop/README.md` records the exact VS edition/version confirmed on each host. The proving host is documented as developer-only. PTT audio path is proven or explicitly deferred with a documented blocker.

### Risks

- **`getUserMedia` in Tauri WebView**: may not function without explicit Tauri `allow` permissions or HTTPS. Test early; if it fails, defer PTT audio to D.3 and record the blocker.
- **`\\?\` path prefix**: `canonicalize()` in Rust adds this prefix on Windows, which breaks `python.exe` invocations. Use `CARGO_MANIFEST_DIR` path joining without canonicalize.
- **VS edition on ARM64**: the ARM64 host has VS Community 2026 (v18); `vswhere` must discover it dynamically. Do not hardcode `\17\BuildTools` or any version segment.
- **Lock contention**: holding `Arc<Mutex<...>>` during the health-wait loop will deadlock the `health_check` command if both are called concurrently. Lock → spawn → release → poll. See `lib.rs` design above.

---

## Sub-Slice D.3 — Resident Session Loop

### Why Here

D.2 proves the durable shell works for a single text turn and, where PTT audio was proven, a voice turn. D.3 closes the gap to continuous interaction without manual re-invocation between turns. If PTT audio was deferred in D.2 with a documented blocker, D.3 must resolve that blocker before voice-turn multi-turn acceptance is claimed.

### Goal

Three or more consecutive voice turns complete in one session on both host classes without any manual re-invocation between turns. The desktop shell's tray and conversation display update continuously.

### Scope

**`backend/app/services/session_service.py`** — new service, created in D.3:
- `SessionService` manages a single active session across turns.
- `start_session() -> str` — creates a session via `SessionManager`; returns `session_id`.
- `end_session()` — closes the active session via `SessionManager`.
- `is_session_active() -> bool`
- Provides the lifecycle boundary for the resident loop: session is created once on app start (or first invocation), persists across turns, closes on app shutdown.
- Injected into the FastAPI app state and exposed via dependency in `api/dependencies.py`.

**Extend `backend/app/api/routes/session.py`**:
- `GET /session/status` → current session id, turn count, state.

**Desktop shell updates (via Tauri commands):**
- Frontend invokes Tauri commands; the Rust/Tauri layer calls backend endpoints. Frontend JS must not call session or turn endpoints directly.
- On app start: Rust layer calls `POST /session/create` (already done by `start_backend` command in D.2) — no additional frontend `fetch()` call needed.
- On each PTT release or text submit: frontend invokes `submit_voice` or `submit_text` Tauri command; Rust layer calls `POST /task/voice` or `POST /task/text`; response appended to conversation display.
- On app close: Rust layer calls `POST /session/close` in the `CloseRequested` handler.
- Conversation display appends each turn sequentially in the same session view.
- Tray reflects `is_session_active` state via `GET /session/status` (new route below), read by a `get_session_status` Tauri command.

**`backend/tests/unit/services/test_session_service.py`**:
- `test_start_session_creates_session_id`
- `test_end_session_marks_inactive`
- `test_is_session_active_reflects_state`

**`backend/tests/runtime/desktop/test_resident_loop_live.py`** — `@pytest.mark.live @pytest.mark.desktop`, `skipif SKIP_UNLESS_LIVE`:
- `test_three_consecutive_turns_in_resident_session_complete_without_error` — via HTTP to running backend; uses text turns (no live mic required for API-level proof); requires_ollama

### Files Not to Modify

- `TurnEngine`, `SessionManager`, `TurnArtifact` — consumed, not changed
- `scripts/run_jarvis.py` — proving host unchanged

### Key Design Decisions

- The resident session is started once and persists across turns. In D.2 `start_backend` already calls `POST /session/create`; D.3 ensures the Rust layer keeps that session open across turns rather than recreating it.
- `SessionService` owns the active session reference; `SessionManager` owns session-level artifact write.
- Working memory flows naturally: `SessionManager.get_working_context()` is called per turn; continuity is a side effect of the persistent session.
- All frontend-to-backend interaction goes through Tauri commands; the Rust layer makes the HTTP calls.

### Acceptance

- Desktop live on both host classes: three or more consecutive turns (text, or voice if PTT was proven in D.2) complete in one session without manual re-invocation; each turn's transcript and response appear in the conversation display.
- API-level resident loop proven: `test_resident_loop_live.py` passes with three text turns via HTTP on both host classes.
- Regression: 74+ tests green.

### Finish Line

JARVISv7 maintains a session across multiple turns without requiring the user to reinitiate. The desktop shell conversation view grows per turn in one session.

### Risks

- Audio device state across turns in the native shell: microphone may hold state between PTT presses. Document whether `sounddevice` or Web Audio API device handling requires explicit release/reacquire between turns. If so, handle it in the shell's PTT release handler.

---

## Sub-Slice D.4 — Wake Integration in Durable Host

### Why Here

PTT is sufficient for D.2/D.3 but is not the primary voice-first invocation model. Wake-word activation wires the B.4 wake runtime into the resident loop so the user can speak "hey jarvis" to invoke the assistant without touching hardware.

### Goal

"Hey jarvis" triggers LISTENING within the durable host on both host classes via openWakeWord. PTT remains available. Wake-unavailable degrades cleanly to PTT-only with a visible indicator.

### Scope

**`backend/app/services/session_service.py`** — extend with wake monitoring:
- `start_wake_monitoring(wake_runtime: WakeBase)` — begins a background thread or coroutine that continuously feeds microphone audio chunks to `wake_runtime.detect()`.
- `stop_wake_monitoring()` — stops the wake monitoring thread cleanly.
- On positive detection: signals the resident loop to transition to LISTENING; queues a voice capture + turn cycle.

**`backend/app/services/voice_service.py`** — extend:
- `stream_audio_chunks(chunk_duration_s: float) -> Iterator[np.ndarray]` — yields small audio chunks for wake monitoring. Used by `session_service.start_wake_monitoring()`.

**Backend API — consume existing D.1 `/status/wake` route:**
- `GET /status/wake` is already defined in D.1 (`routes/status.py`). D.4 extends the behavior behind it: the `session_service.start_wake_monitoring()` method populates the `available` and `reason` fields returned by this route. No new endpoint is introduced.

**Desktop shell updates (via Tauri commands):**
- Frontend invokes a `get_wake_status` Tauri command (which calls `GET /status/wake`) on startup and periodically to determine wake availability.
- When wake is unavailable, display a PTT-only indicator; when available, the wake loop drives LISTENING automatically and PTT remains as manual override.

**`backend/tests/unit/services/test_session_service.py`** — extend:
- `test_wake_monitoring_start_and_stop_clean`
- `test_wake_detection_positive_triggers_listening_transition` (mock `WakeBase`)
- `test_wake_unavailable_degrades_to_ptt_only_without_error`

**`backend/tests/runtime/desktop/test_wake_integration_live.py`** — `@pytest.mark.live @pytest.mark.desktop @pytest.mark.wake`, `skipif SKIP_UNLESS_LIVE`:
- `test_wake_monitoring_starts_and_stops_cleanly_on_current_host`
- `test_hey_jarvis_fixture_triggers_detection_in_resident_loop` — uses `hey_jarvis_fixture.wav` fed as a simulated chunk; does not require open microphone

### Files Not to Modify

- `backend/app/runtimes/wake/**` — wake runtimes consumed, not changed
- `BargeInDetector` — barge-in remains mic-during-speech; wake-during-speech is documented as future polish (not in D.4 scope)
- `TurnEngine`, `SessionManager`, `TurnArtifact`

### Key Design Decisions

- Wake monitoring runs in a daemon thread managed by `SessionService`. It does not block the main backend event loop.
- Wake-word detection (openWakeWord) and barge-in detection (BargeInDetector) are separate responsibilities. Wake fires before a turn begins; barge-in fires during SPEAKING. They do not share infrastructure.
- Wake-during-SPEAKING (barge-by-wake) is explicitly out of scope for D.4. If wake fires while the assistant is speaking, the detection is ignored until the assistant returns to IDLE. Document this clearly in `session_service.py`.
- The thread owns audio chunk feeding; `voice_service.stream_audio_chunks()` is the only entry point for chunk streaming.

### Acceptance

- Desktop live on both host classes: speaking "hey jarvis" triggers LISTENING without PTT on both host classes where openWakeWord models are present and mic is available.
- Wake-unavailable closeout condition: if openWakeWord models are missing or the host mic is unavailable, `GET /status/wake` returns `available: false`; shell shows PTT-only indicator; no crash. PTT-only is an accepted D.4 closeout state only when live wake detection is genuinely not achievable on that host. If models are present and mic is available, live wake trigger is required.
- Regression: 74+ tests green.

### Finish Line

JARVISv7 listens for the wake phrase continuously in the resident loop. PTT remains available as a manual override. Wake-unavailable degrades cleanly.

### Risks

- **Microphone contention**: wake monitoring holds an audio stream open continuously. When a turn starts (LISTENING), the wake stream must yield the microphone without device conflict. Verify that `sounddevice` supports concurrent stream management or that a single stream is shared. Document the chosen strategy before implementation.
- **openWakeWord latency on ARM64**: `onnxruntime` on ARM64 may have higher inference latency per chunk. Verify that a 0.1–0.2 s chunk interval keeps detection responsive. If not, increase chunk size and document the sensitivity trade-off.

---

## Sub-Slice D.5 — Personality / Presence Polish

### Why Here

The application is fully functional after D.4. D.5 makes it feel intentionally designed: the assistant's personality is visibly selectable, responses reflect the chosen profile, and presence behaviors give the assistant a consistent character across turns.

### Goal

A user can select a personality profile in the shell; the next turn's response style reflects that profile. Acknowledgment phrases and response pacing vary per profile.

### Scope

**Backend — personality extension:**
- `backend/app/personality/adapter.py` — promote from stub to real implementation:
  - `apply_personality(prompt: str, profile: PersonalityProfile) -> str` — appends tone/brevity/formality guidance to the prompt based on the active profile's fields. Keeps guidance concise; does not add tool or memory instructions.
  - Uses `system_prompt_addendum` from the profile when non-empty.
- `config/personality/` — add at minimum one additional personality profile YAML alongside `default.yaml`:
  - `concise.yaml` — shorter, more direct response style.
  - `warm.yaml` — warmer, more conversational tone.

**Backend API extension:**
- `GET /personality/list` → list of available profile ids and display names from `config/personality/*.yaml`.
- `POST /personality/select` → `{profile_id: str}` — switches the active personality for subsequent turns; returns the new active profile summary.

**Desktop shell updates — `desktop/src/components/`:**
- `personality-selection/` — new component: dropdown or button group showing available profiles; calls `POST /personality/select` on change; shows current active profile.
- Presence behaviors in the conversation display: assistant acknowledgment phrases ("Understood.", "On it.", "Let me think.") appear as UI-side text injected at LISTENING/TRANSCRIBING/REASONING state transitions before the actual response arrives. These are UI-layer latency bridges, not LLM output.
- Response pacing: if the TTS sample rate is exposed in `VoiceTurnResponse`, the shell may use it as a visual cue; otherwise, presence is a fixed UI pattern.

**`backend/tests/unit/personality/test_adapter.py`** — extend or add:
- `test_adapter_appends_tone_guidance_to_prompt`
- `test_adapter_respects_brevity_field`
- `test_adapter_uses_system_prompt_addendum_when_set`
- `test_adapter_returns_prompt_unchanged_when_profile_has_no_addendum`

**`backend/tests/unit/api/test_routes.py`** — extend:
- `test_personality_list_returns_available_profiles`
- `test_personality_select_switches_active_profile` (mock)

### Files Not to Modify

- `backend/app/cognition/prompt_assembler.py` — personality is already injected as a parameter; adapter enriches the profile, does not change the assembler interface
- `backend/app/conversation/engine.py` — no changes; personality flows through existing injection
- `TurnArtifact` schema — `active_personality_profile_id` field already present

### Key Design Decisions

- Personality switch applies to the next turn, not the current one. The active profile is stored in `SessionService` state and used by `TurnEngine` on each `run_*_turn` call.
- `personality/adapter.py` uses the existing personality injection seam in the turn engine. The adapter enriches the assembled prompt using the `PersonalityProfile` fields already passed to `assemble_prompt`. It must not modify `prompt_assembler.py`. If the existing seam cannot apply profile guidance without changing the assembler interface, stop and surface a narrow correction before proceeding.
- Adapter correctness is verified by unit tests against the existing seam, not by asserting LLM output content.
- Presence behaviors (acknowledgment phrases) are entirely shell-side UI text. They are not LLM output, not persisted to artifacts, and not part of the turn lifecycle. The shell displays them during state transitions as user-facing latency feedback.
- New personality YAML files are additive config. Adding a new profile requires only a new YAML file and no code change.

### Acceptance

- Backend unit: `test_adapter_*` suite green on both host classes.
- Desktop live on both host classes: selecting "concise" profile → next turn response is observably shorter than "default"; selecting "warm" profile → response is observably more conversational.
- Presence behaviors: acknowledgment text appears in the conversation display during LISTENING/TRANSCRIBING/REASONING states before the LLM response arrives.
- Regression: 74+ tests green.

### Finish Line

JARVISv7 presents a consistent, selectable personality across turns. Presence behaviors give the assistant character during inference latency. No cognition changes, no tools, no agents.

### Risks

- LLM response style variation per personality is non-deterministic. Acceptance requires an observable subjective difference in a live turn, not a unit-provable assertion. Document this explicitly in the D.5 CHANGE_LOG evidence: a live turn demonstrating profile-switched behavior is the evidence, not a unit assertion on LLM output.

---

## File Placement Summary

| File | Action | Sub-slice |
|------|---------|-----------|
| `scripts/run_backend.py` | New (if absent) | D.1 |
| `backend/app/api/__init__.py` | New | D.1 |
| `backend/app/api/dependencies.py` | New | D.1 |
| `backend/app/api/schemas/{common,readiness,session,task,voice}.py` | New | D.1 |
| `backend/app/api/routes/{health,readiness,session,task,voice,diagnostics}.py` | New | D.1 |
| `backend/app/api/routes/personality.py` | New | D.5 |
| `desktop/src-tauri/{main.rs,lib.rs,backend.rs,tray.rs,Cargo.toml,tauri.conf.json,build.rs}` | New | D.2 |
| `desktop/src/{index.html,main.js,style.css}` | New | D.2 |
| `desktop/src/components/{status/,hardware-selection/,conversation/,llm-selection/}` | New | D.2 |
| `desktop/src/components/personality-selection/` | New | D.5 |
| `desktop/README.md` | Extend (arch toolchain) | D.2 |
| `README.md` | Extend (arch build prerequisites) | D.2 |
| `backend/app/services/session_service.py` | New | D.3 |
| `backend/app/services/voice_service.py` | Extend (stream_audio_chunks) | D.4 |
| `backend/app/personality/adapter.py` | Promote from stub | D.5 |
| `config/personality/concise.yaml` | New | D.5 |
| `config/personality/warm.yaml` | New | D.5 |
| `backend/tests/unit/api/test_routes.py` | New | D.1, D.5 |
| `backend/tests/integration/api/test_headless_client.py` | New | D.1 |
| `backend/tests/unit/services/test_session_service.py` | New + extend | D.3, D.4 |
| `backend/tests/unit/personality/test_adapter.py` | New/extend | D.5 |
| `backend/tests/runtime/desktop/test_resident_loop_live.py` | New | D.3 |
| `backend/tests/runtime/desktop/test_wake_integration_live.py` | New | D.4 |
| `CHANGE_LOG.md` | Append per sub-slice | D.1–D.5 |
| `SYSTEM_INVENTORY.md` | Append at Slice D closeout | D.5 |

**Must not create:**
- `backend/app/cache/**`, `backend/app/tools/**`, `backend/app/memory/episodic.py`, `backend/app/memory/retrieval.py`
- `backend/app/agents/**`
- Any LLM runtime file changes (H.1 scope)
- QNN inference code (H.2 scope)
- Redis/SearXNG/docker-compose changes (Group E scope)
- Any file in `backend/app/cognition/` beyond what personality adapter requires

---

## Slice-Level Closeout Criteria

Slice D is complete only when all of the following are true:

1. Pre-D.1 gates (D-G4, D-G5) verified and documented before D.1 begins.
2. Backend HTTP API serves all D.1 routes; headless client drives a full text turn without any shell code.
3. Native Tauri desktop app builds on Windows x64 using x64 toolchain.
4. Native Tauri desktop app builds on Windows ARM64 using ARM64 toolchain (MSVC v143 ARM64 build tools confirmed).
5. Desktop live: launch → tray → PTT → speak → transcript + response in conversation display → tray state transitions per turn.
6. Hardware selection display shows active family + device per STT/TTS/LLM/Wake from `/readiness`.
7. Three consecutive turns complete in one resident session without manual re-invocation on both host classes.
8. Wake phrase "hey jarvis" triggers LISTENING without PTT on both host classes via openWakeWord (D.4); or wake-unavailable degrades to PTT-only with visible indicator.
9. Personality profile switch visible in shell; next turn response observably reflects the selected profile.
10. All unit suites green on both host classes.
11. `validate_backend.py regression` green on both host classes (count ≥ 74).
12. `CHANGE_LOG.md` has one entry per sub-slice D.1–D.5 with host class(es).
13. No Group E/F/G/H/I file introduced at Slice D closeout.
14. Slice A+B+C regression count has not decreased.

---

## Evidence and Inventory Expectations

- `CHANGE_LOG.md`: one entry per sub-slice immediately after validation evidence is confirmed for that sub-slice on both host classes.
- `SYSTEM_INVENTORY.md`: one entry at Slice D closeout (after acceptance criteria 10 and 11 are both met). State: `Verified`.

Inventory entry should note:
- Capability: Slice D durable application surface (D.1–D.5)
- Location: `backend/app/api/`, `backend/app/services/session_service.py`, `backend/app/personality/adapter.py`, `desktop/src/`, `desktop/src-tauri/`, `config/personality/`
- Validation: native builds on Windows x64 and Windows ARM64; headless client integration PASS; resident loop live PASS on both host classes; wake integration PASS or SKIP-with-reason on both host classes; regression ≥ 74 on both host classes
- Explicit caveats: PTT audio capture in D.2 is the primary path; acoustic barge-in remains deterministically tested only (C.5 boundary preserved); llama.cpp deferred H.1; QNN STT deferred H.2; no tools, agents, episodic memory, or Group E/F/G/H/I introduced

---

## Finish Line

Group D is done when JARVISv7 runs as a native desktop application on both Windows host classes, maintains a session across multiple turns, responds to the wake phrase, and presents a selectable personality — all through a thin shell that consumes an explicit backend HTTP contract. The proving host (`scripts/run_jarvis.py`) remains available for diagnostics; `desktop/` is the durable surface. Group E substrate work begins only after this gate is green.
