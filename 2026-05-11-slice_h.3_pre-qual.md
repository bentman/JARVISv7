# 2026-05-11-slice_h.3_pre-qual.md

## H.3 Pre-Qualification — ARM64 QNN STT Artifact and Runtime Contract Proof

**Date:** 2026-05-11  
**Status:** Proposed  
**Blocks:** Sub-Slice H.3 — ARM64 QNN STT Activation  
**Primary host:** Windows ARM64 / Snapdragon X Elite  
**Execution boundary:** Repo-root Agent session only  
**Repo rule:** No source, test, config, provisioning, model-catalog, slice-doc, `SYSTEM_INVENTORY.md`, or `.gitignore` changes during this task.

---

## Purpose

H.3 previously failed because the Qualcomm Whisper artifact/runtime contract was not proven before runtime wiring began.

This pre-qualification proves only whether H.3 is ready to proceed. It does not implement H.3.

The result must be exactly one of:

- `PROCEED`
- `DEFER - <named stop condition>`

---

## Non-Negotiable Boundary

All work must be performed from repo root in the Agent-bound repo workspace.

Do not stage artifacts outside the repo workspace. 

Use the repo-visible ignored scratch path:

    cache/temp/h3prequal/

Required subfolders:

    cache/temp/h3prequal/artifacts/
    cache/temp/h3prequal/logs/
    cache/temp/h3prequal/notes/
    cache/temp/h3prequal/probe_scripts/

This path is inside the repo workspace so Agent can inspect and reuse it during the same task, but it is not model-catalog/runtime wiring.

Do not stage artifacts directly into final repo model locations.

---

## Correct Acquisition Path

Primary path:

- Download Qualcomm’s already precompiled Whisper-Base artifact:
  - Model: `qualcomm/Whisper-Base`
  - Runtime: `PRECOMPILED_QNN_ONNX`
  - Chipset: `Snapdragon X Elite`
  - Release asset: `whisper_base-precompiled_qnn_onnx-float-qualcomm_snapdragon_x_elite.zip`

Do not use AI Hub export/compile tooling as the primary path.

Fallback path:

- Use `qai_hub_models` export tooling only if the precompiled Qualcomm artifact is unavailable or unsuitable.
- That fallback is outside this pre-qual and requires a separate decision.

---

## Current Repo Runtime Mechanism

The repo does not require a standalone `QAIRT_SDK_PATH` for this path.

The current QNN provider path is:

- import `onnxruntime_qnn`
- use `onnxruntime_qnn.get_library_path()`
- use `onnxruntime_qnn.get_qnn_htp_path()`
- register/use `QNNExecutionProvider`
- pass `backend_path` through provider options
- disable CPU fallback during proof sessions

Step 0 proves this repo mechanism before any artifact work.

---

## Required Repo Provisioning

Use the repo-owned provisioning path.

Do not manually install packages into `backend/.venv` during this pre-qual.

From repo root:

    backend\.venv\Scripts\python scripts\provision.py install

The ARM64 QNN extra is expected to provide:

- `onnxruntime`
- `onnxruntime-qnn`
- `onnx`

The repo base is expected to provide general runtime dependencies such as:

- `numpy`
- `soundfile`
- `huggingface_hub`

Known probe dependency gap:

- `transformers` is not currently a declared repo dependency.
- `transformers` may be needed for `WhisperFeatureExtractor` during the known-audio mel probe.
- Do not add it to `pyproject.toml` during this pre-qual.
- Do not silently install it into `backend/.venv`.
- If the known-audio probe cannot run without it, stop with:

    `DEFER - probe-dependency-missing`

---

## Required Fixture

Known-audio fixture:

    backend/tests/fixtures/hello_world.wav

Expected spoken content:

    hello world

This pre-qual does not require a correct transcript. It only attempts a mechanical proof through:

    audio fixture
    -> mel/features
    -> encoder run
    -> one decoder step
    -> logits shape

Full greedy decode and transcript correctness belong to H.3 implementation.

---

## Step 0 — ARM64 QNN Provider Gate

Run on the ARM64 repo host from repo root.

### 0.1 Confirm QNN plugin/backend discovery

    backend\.venv\Scripts\python -c "import platform; from pathlib import Path; import onnxruntime, onnxruntime_qnn; plugin=Path(onnxruntime_qnn.get_library_path()); htp=Path(onnxruntime_qnn.get_qnn_htp_path()); print('arch=', platform.machine()); print('qnn_plugin=', plugin, plugin.is_file()); print('qnn_htp=', htp, htp.is_file()); print('providers=', onnxruntime.get_available_providers())"

Expected:

- architecture is ARM64
- QNN plugin path exists
- QNN HTP backend path exists
- QNN provider is visible or can be registered by the repo live gate

### 0.2 Run H.2 live gate

    $env:JARVISV7_LIVE_TESTS="true"
    backend\.venv\Scripts\python -m pytest backend/tests/runtime/hardware/test_qnn_gate_live.py -q -s

Expected:

    2 passed

Step 0 proves only:

- QNN/HTP library discoverability
- QNN EP registration
- current H.2 live gate remains valid

It does not prove the Whisper artifact.

Stop if this fails:

    DEFER - qnn-provider-gate-failed

---

## Step 1 — Prepare Repo-Local Scratch Workspace

Run from repo root.

    New-Item -ItemType Directory -Force -Path cache\temp\h3prequal\artifacts | Out-Null
    New-Item -ItemType Directory -Force -Path cache\temp\h3prequal\logs | Out-Null
    New-Item -ItemType Directory -Force -Path cache\temp\h3prequal\notes | Out-Null
    New-Item -ItemType Directory -Force -Path cache\temp\h3prequal\probe_scripts | Out-Null

Record:

- repo root path
- scratch root path
- confirmation that scratch root is under the repo workspace
- confirmation that no model-catalog/runtime location is being used

Stop if the scratch path cannot be created inside the repo workspace:

    DEFER - staging-boundary-invalid

---

## Step 2 — Download Qualcomm Precompiled Artifact

Run from repo root.

Use the public Qualcomm release asset:

    https://qaihub-public-assets.s3.us-west-2.amazonaws.com/qai-hub-models/models/whisper_base/releases/v0.53.1/whisper_base-precompiled_qnn_onnx-float-qualcomm_snapdragon_x_elite.zip

Download to:

    cache/temp/h3prequal/artifacts/whisper_base-precompiled_qnn_onnx-float-qualcomm_snapdragon_x_elite.zip

Example command:

    Invoke-WebRequest `
      -Uri "https://qaihub-public-assets.s3.us-west-2.amazonaws.com/qai-hub-models/models/whisper_base/releases/v0.53.1/whisper_base-precompiled_qnn_onnx-float-qualcomm_snapdragon_x_elite.zip" `
      -OutFile "cache\temp\h3prequal\artifacts\whisper_base-precompiled_qnn_onnx-float-qualcomm_snapdragon_x_elite.zip"

Record:

- source URL
- file name
- file size
- download date/time
- local repo-relative artifact path

Stop if download fails:

    DEFER - artifact-download-failed

---

## Step 3 — Inspect Artifact Layout

Extract to:

    cache/temp/h3prequal/artifacts/whisper_base_x_elite/

Commands:

    New-Item -ItemType Directory -Force -Path cache\temp\h3prequal\artifacts\whisper_base_x_elite | Out-Null

    Expand-Archive `
      -Force `
      -Path "cache\temp\h3prequal\artifacts\whisper_base-precompiled_qnn_onnx-float-qualcomm_snapdragon_x_elite.zip" `
      -DestinationPath "cache\temp\h3prequal\artifacts\whisper_base_x_elite"

    Get-ChildItem -Recurse "cache\temp\h3prequal\artifacts\whisper_base_x_elite" |
      Select-Object FullName, Length |
      Format-Table -AutoSize

Expected minimum:

- one encoder ONNX file
- one decoder ONNX file
- both files are non-zero size

Do not assume names.

Common names may include, but are not guaranteed to be:

- `WhisperEncoder.onnx`
- `WhisperDecoder.onnx`
- `HfWhisperEncoder.onnx`
- `HfWhisperDecoder.onnx`

Record:

- exact extracted encoder ONNX file path
- exact extracted decoder ONNX file path
- file sizes
- any additional files that appear relevant to runtime contract

Stop if encoder/decoder files are missing:

    DEFER - artifact-layout-mismatch

---

## Step 4 — Prove QNN Session Initialization

Run on ARM64 repo host from repo root.

Use the actual encoder and decoder paths discovered in Step 3.

Do not hard-code guessed names.

### 4.1 Encoder session

Example shape only; replace the path with the actual discovered encoder path:

    backend\.venv\Scripts\python -c "from pathlib import Path; from backend.app.hardware.qnn_provider import create_qnn_session; p=Path(r'cache\temp\h3prequal\artifacts\whisper_base_x_elite\<ACTUAL_ENCODER_NAME>.onnx'); s,m=create_qnn_session(p, disable_cpu_fallback=True); print('encoder providers=', s.get_providers()); print('encoder method=', m); assert 'QNNExecutionProvider' in s.get_providers(); print('ENCODER SESSION INIT: PASS')"

### 4.2 Decoder session

Example shape only; replace the path with the actual discovered decoder path:

    backend\.venv\Scripts\python -c "from pathlib import Path; from backend.app.hardware.qnn_provider import create_qnn_session; p=Path(r'cache\temp\h3prequal\artifacts\whisper_base_x_elite\<ACTUAL_DECODER_NAME>.onnx'); s,m=create_qnn_session(p, disable_cpu_fallback=True); print('decoder providers=', s.get_providers()); print('decoder method=', m); assert 'QNNExecutionProvider' in s.get_providers(); print('DECODER SESSION INIT: PASS')"

Pass condition:

- encoder session initializes
- decoder session initializes
- `QNNExecutionProvider` is present
- CPU fallback remains disabled

Record:

- encoder providers
- encoder initialization method
- decoder providers
- decoder initialization method

Stop if either session fails or falls back to CPU:

    DEFER - qnn-session-init-failed

---

## Step 5 — Capture Tensor Contract

Run on ARM64 repo host from repo root.

Use the actual encoder and decoder paths discovered in Step 3.

Do not hard-code guessed names.

### 5.1 Encoder contract

Example shape only; replace the path with the actual discovered encoder path:

    backend\.venv\Scripts\python -c "from pathlib import Path; from backend.app.hardware.qnn_provider import create_qnn_session; p=Path(r'cache\temp\h3prequal\artifacts\whisper_base_x_elite\<ACTUAL_ENCODER_NAME>.onnx'); s,_=create_qnn_session(p, disable_cpu_fallback=True); print('=== ENCODER INPUTS ==='); [print(f'name={i.name!r} shape={i.shape} dtype={i.type}') for i in s.get_inputs()]; print('=== ENCODER OUTPUTS ==='); [print(f'name={o.name!r} shape={o.shape} dtype={o.type}') for o in s.get_outputs()]"

### 5.2 Decoder contract

Example shape only; replace the path with the actual discovered decoder path:

    backend\.venv\Scripts\python -c "from pathlib import Path; from backend.app.hardware.qnn_provider import create_qnn_session; p=Path(r'cache\temp\h3prequal\artifacts\whisper_base_x_elite\<ACTUAL_DECODER_NAME>.onnx'); s,_=create_qnn_session(p, disable_cpu_fallback=True); print('=== DECODER INPUTS ==='); [print(f'name={i.name!r} shape={i.shape} dtype={i.type}') for i in s.get_inputs()]; print('=== DECODER OUTPUTS ==='); [print(f'name={o.name!r} shape={o.shape} dtype={o.type}') for o in s.get_outputs()]"

Record:

- every encoder input name
- every encoder input shape
- every encoder input dtype
- every encoder output name
- every encoder output shape
- every encoder output dtype
- every decoder input name
- every decoder input shape
- every decoder input dtype
- every decoder output name
- every decoder output shape
- every decoder output dtype
- KV-cache input names and shapes, if present
- logits output name and shape

Stop if tensor contracts cannot be captured:

    DEFER - tensor-contract-missing

Stop if decoder KV-cache requirements are present but unclear:

    DEFER - kvcache-contract-unknown

---

## Step 6 — Known-Audio Mechanical Probe

Run on ARM64 repo host from repo root.

Fixture:

    backend/tests/fixtures/hello_world.wav

This step attempts mechanical inference only:

    hello_world.wav
    -> mel/features
    -> encoder run
    -> decoder one step
    -> logits shape

A correct transcript is not required.

### 6.1 Check probe dependency

    backend\.venv\Scripts\python -c "import importlib.util; print('transformers_available=', importlib.util.find_spec('transformers') is not None)"

If `transformers_available=False`, stop:

    DEFER - probe-dependency-missing

Do not install `transformers` in this pre-qual task.

### 6.2 Create temporary one-step probe

Create the temporary probe script under:

    cache/temp/h3prequal/probe_scripts/probe_whisper_qnn_step.py

The script may use:

- `wave`
- `numpy`
- `transformers.WhisperFeatureExtractor`
- `backend.app.hardware.qnn_provider.create_qnn_session`

The script must:

1. load `backend/tests/fixtures/hello_world.wav`
2. produce Whisper input features
3. run the encoder
4. initialize decoder inputs using names/shapes from Step 5
5. run one decoder step
6. print:
   - audio shape and sample rate
   - mel/input feature shape and dtype
   - encoder provider list
   - encoder output shape
   - decoder provider list
   - decoder logits shape
   - initialization method for each session

Pass condition:

- mel/features produced
- encoder runs
- decoder runs one step
- logits shape is plausible
- QNN provider is used for both sessions

Stop if this fails:

    DEFER - decode-step-failed

---

## Step 7 — Regression Check

Run on ARM64 repo host from repo root.

    backend\.venv\Scripts\python scripts\validate_backend.py regression

Expected current baseline:

    102 passed, 2 deselected

Stop if regression drops:

    DEFER - regression-failed

---

## Acceptance Criteria

All must pass for `PROCEED`:

| ID | Criterion |
|---|---|
| PQ-1 | ARM64 QNN plugin/backend discovery succeeds through `onnxruntime-qnn` |
| PQ-2 | `test_qnn_gate_live.py` returns `2 passed` |
| PQ-3 | Scratch staging is repo-local under `cache/temp/h3prequal/` |
| PQ-4 | Qualcomm Snapdragon X Elite precompiled QNN ONNX artifact downloads |
| PQ-5 | Encoder and decoder ONNX files are present and non-zero |
| PQ-6 | Encoder QNN session initializes with CPU fallback disabled |
| PQ-7 | Decoder QNN session initializes with CPU fallback disabled |
| PQ-8 | Encoder tensor contract is fully recorded |
| PQ-9 | Decoder tensor contract is fully recorded, including KV-cache inputs if present |
| PQ-10 | Known-audio mechanical probe runs through encoder |
| PQ-11 | Decoder one-step probe produces plausible logits |
| PQ-12 | Regression remains green |

---

## Stop Conditions

| Failure | Stop reason |
|---|---|
| QNN plugin/backend discovery fails | `DEFER - qnn-provider-gate-failed` |
| H.2 live gate does not return `2 passed` | `DEFER - qnn-provider-gate-failed` |
| Scratch staging is not repo-local | `DEFER - staging-boundary-invalid` |
| Precompiled artifact cannot be downloaded | `DEFER - artifact-download-failed` |
| Encoder/decoder ONNX files are missing | `DEFER - artifact-layout-mismatch` |
| Encoder/decoder session fails or uses CPU fallback | `DEFER - qnn-session-init-failed` |
| Tensor contract cannot be captured | `DEFER - tensor-contract-missing` |
| Decoder KV-cache shapes are unclear | `DEFER - kvcache-contract-unknown` |
| `transformers` is unavailable for known-audio probe | `DEFER - probe-dependency-missing` |
| One-step decoder probe fails | `DEFER - decode-step-failed` |
| Regression fails | `DEFER - regression-failed` |

---

## Output Required

Before any H.3 implementation work, record:

1. repo root path
2. scratch root path
3. QNN plugin path and HTP backend path from Step 0
4. H.2 live gate result
5. downloaded artifact URL, file name, size, and repo-relative staging path
6. exact extracted encoder/decoder file names and sizes
7. encoder session-init result and method
8. decoder session-init result and method
9. complete encoder tensor contract
10. complete decoder tensor contract
11. known-audio probe result, including encoder output shape and decoder logits shape
12. regression result
13. final decision:
    - `PROCEED`
    - or `DEFER - <named stop condition>`

---

## Final Decision Rule

Return:

    PROCEED

only when PQ-1 through PQ-12 pass.

Return:

    DEFER - <named stop condition>

when any required proof fails.

Do not continue into H.3 implementation work during this pre-qualification.

Do not modify source, tests, config, provisioning, model catalog, `SYSTEM_INVENTORY.md`, slice docs, or `.gitignore` during this pre-qualification.