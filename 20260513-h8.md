# 20260513-h8.md
## Sub-Slice H.8 — ARM64 Follow-up Validation Prompt

Objective: run H.8 validation on a Windows ARM64 host using the current repo state after x64 H.8 implementation.

Constraints:
- Follow `AGENTS.md` rules (venv, deterministic commands, one command at a time, evidence-first reporting).
- Do not edit runtime source files for this pass.
- Scope is validation + evidence capture for ARM64 host outcomes.

Read first:
- `AGENTS.md`
- `20260505-slice_h.md` (H.8 section)
- `CHANGE_LOG.md` (latest H.5/H.7/H.8-related entries)

Focus files (read/execute only unless a bounded corrective fix is required):
- `backend/tests/runtime/turn/test_voice_acceleration_matrix_live.py`
- `backend/tests/runtime/acceleration_matrix/test_acceleration_matrix.py`

## What x64 already proved (context)
- H.8 live turn matrix test passes on x64.
- H.8 acceleration matrix test passes on x64.
- Regression remains green.
- TTS accelerated paths are currently Deferred (`provider-override-missing`) per H.7.
- DirectML remains non-proven (`SKIP-prereq-missing` / deferred path outcomes).

## ARM64 task
1. Validate H.8 runtime matrix test on ARM64.
2. Validate H.8 acceleration matrix test on ARM64.
3. Validate backend regression on ARM64.
4. Report outcomes with explicit close-state language for ARM64 rows.

## Commands (run exactly, one at a time)
1. `$env:JARVISV7_LIVE_TESTS="true"; backend/.venv/Scripts/python -m pytest backend/tests/runtime/turn/test_voice_acceleration_matrix_live.py -q`
2. `$env:JARVISV7_LIVE_TESTS="true"; backend/.venv/Scripts/python -m pytest backend/tests/runtime/acceleration_matrix/test_acceleration_matrix.py -q`
3. `backend/.venv/Scripts/python scripts/validate_backend.py regression`

## Expected reporting format
- Summary
- Host class validated on
- Files inspected/used
- Commands executed and outcomes (`PASS` / `FAIL` / `SKIPPED`)
- Minimal evidence excerpts
- Final status (include whether H.8 is ARM64-validated or requires follow-up)

## Stop conditions
- If any `PASS`-rated path fails in full-turn validation, stop and report (do not claim H.8 closeout).
- If failure mode is deterministic/stale assertion, you may apply up to 2 bounded corrective fixes, re-run the same commands, and report both pre-fix and post-fix outcomes.