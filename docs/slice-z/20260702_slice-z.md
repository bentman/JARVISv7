# Slice Z — Product Readiness and Operational Polish

Date: 2026-07-02

## Purpose

Slice Z is a stabilization slice. It turns the existing working project into a more coherent repo-run product preview by smoothing startup, launch, readiness, diagnostics, artifacts, desktop state, validation, and documentation.

This slice is not a new core engineering milestone. It should reduce friction, reduce duplicated startup logic, improve user-visible truth, and make the current system easier to start, inspect, and trust.

## General contract

All work items share this contract:

- Preserve existing runtime behavior unless a work item explicitly corrects a product-facing gap.
- Prefer existing modules, scripts, commands, and UI surfaces.
- Keep changes small, observable, and reversible.
- Treat the desktop shell and backend API as the product preview surface.
- Treat `scripts/` as operator/developer entry points, not a second product architecture.
- Improve clarity before adding capability.
- Update tests only where they prove changed behavior or protect a simplified path.
- Update `SYSTEM_INVENTORY.md` and `CHANGE_LOG.md` only after validated observable changes.

## Out of scope

Slice Z does not include:

- semantic/vector memory
- autonomous agents
- new model families
- new accelerator support
- new runtime families
- full installer/distribution pipeline
- major desktop redesign
- broad test-suite expansion
- cloud-provider expansion
- new service substrate
- new governance framework

## Parent acceptance

Slice Z is acceptable when the current repo-run product preview has one clear launch path, accurate readiness/degraded status, useful desktop/backend diagnostics, minimally useful conversation debug detail for real turns, personality profiles that visibly affect local turns through the real prompt/runtime path, and one focused product smoke validation path.

## Work items

| Slice | Capability / feature | Goal | Main surface area | Acceptance |
|---|---|---|---|---|
| Z.1 | Startup context cleanup | Centralize startup facts so scripts and API stop rebuilding profile, extras, preflight, and readiness differently. | `backend/app/api/app.py`, `backend/app/hardware/`, `backend/app/services/`, `scripts/bootstrap.py`, `scripts/run_backend.py`, `scripts/run_jarvis.py`, `scripts/validate_backend.py`, related script/API tests. | One shared startup/readiness helper is used by the main startup paths; output remains equivalent or clearer. |
| Z.2 | Readiness summary cleanup | Make readiness easier to understand: what works, what is degraded, why, and what matters for the current product path. | `backend/app/api/routes/readiness.py`, `backend/app/api/schemas/readiness.py`, `backend/app/api/service_status.py`, `desktop/src/components/readiness-panel.js`, `desktop/src/components/degraded-list.js`, `scripts/validate_backend.py`. | `/readiness`, desktop readiness, and profile validation agree on family/device/service state and reasons without noisy non-selected-path detail. |
| Z.3 | Conversation debug and turn artifact contract | Convert the existing Conversation-area debug detail from voice-only to conversation-level status, and expose only the minimal latest-turn/session/artifact facts needed to debug text and voice turns. | `backend/app/api/routes/session.py`, `backend/app/api/schemas/session.py`, `backend/app/services/session_service.py`, `backend/app/services/resident_voice_invocation.py`, `backend/app/artifacts/turn_artifact.py`, `backend/app/conversation/engine.py`, `backend/app/conversation/session_manager.py`, `backend/app/artifacts/storage.py`, `desktop/src/index.html`, `desktop/src/main.js`, `desktop/src/components/resident-voice.js`, desktop static/API tests. | Existing Conversation debug detail covers text and voice latest-turn state using compact observed fields; turn artifacts/status expose failure/degraded/runtime context where available without duplicating normal conversation content. |
| Z.4 | Desktop state smoothing | Reduce ambiguity around starting, ready, degraded, listening, reasoning, speaking, failed, and backend-unavailable states. | `desktop/src/main.js`, `desktop/src/components/state-label.js`, `desktop/src/components/resident-voice.js`, `desktop/src/components/wake-indicator.js`, `desktop/src/style.css`, desktop static tests. | Desktop state labels and controls reflect backend/session reality without contradictory states. |
| Z.5 | Personality check-up | Verify that selected personality profiles alter the actual prompt and local LLM request path enough to create observable style differences across llama.cpp and Ollama. | `config/personality/`, `backend/app/personality/schema.py`, `backend/app/personality/loader.py`, `backend/app/personality/policy.py`, `backend/app/cognition/prompt_assembler.py`, `backend/app/cognition/prompt_renderer.py`, `backend/app/runtimes/llm/base.py`, `backend/app/runtimes/llm/local_runtime.py`, `backend/app/runtimes/llm/ollama_runtime.py`, `backend/app/api/routes/personality.py`, `backend/app/api/schemas/personality.py`, `desktop/src/main.js`, adjacent personality/runtime/API/desktop tests. | Personality selection is visible in desktop and materially affects the generated prompt/request sent to local runtimes; invalid profile files do not 500 the whole profile list; unused personality fields are either wired into the compiled policy or removed from the contract; llama.cpp and Ollama both receive role-separated persona instructions through supported request shapes. |
| Z.6 | Desktop backend diagnostics | Surface backend startup failure details and log paths in the desktop instead of hiding them behind generic launch errors. | `desktop/src-tauri/src/backend.rs`, `desktop/src-tauri/src/lib.rs`, `desktop/src/api-client.js`, `desktop/src/main.js`, `reports/backend_startup.log`, `reports/backend_spawn_stderr.log`, desktop static tests. | Backend launch failures show python path, script path, working directory, log paths, and useful tail output. |
| Z.7 | Product smoke validator | Add one small product-path validation command. | `scripts/validate_backend.py`, `backend/tests/runtime/` or focused product smoke location, `backend/tests/unit/scripts/`, `reports/validation/`. | Validator proves backend startup, readiness load, session create, text turn, and artifact existence without running broad live hardware paths. |
| Z.8 | QuickStart tightening | Keep setup docs centered on the existing desktop launch path, with developer commands secondary. | `docs/QuickStart.md`, `README.md`. | A fresh user sees the shortest supported setup/check/launch path before advanced validation commands. |
| Z.9 | Packaging posture clarification | State clearly that this milestone is repo-run desktop preview, not installer-grade distribution. | `README.md`, `docs/QuickStart.md`, `desktop/src-tauri/tauri.conf.json`, `desktop/package.json`. | Docs and desktop metadata no longer imply a packaged consumer installer is ready. |
| Z.10 | Desktop JS cleanup | Split large top-level desktop state/timer/event wiring only where it reduces maintenance risk. | `desktop/src/main.js`, `desktop/src/api-client.js`, `desktop/src/components/`, desktop static tests. | Main desktop file is smaller and clearer without changing UI behavior. |
| Z.11 | Rust backend bridge cleanup | Deduplicate repeated HTTP request/error patterns in the Tauri backend bridge. | `desktop/src-tauri/src/backend.rs`, `desktop/src-tauri/src/lib.rs`, desktop static tests and build check. | Backend bridge keeps the same commands and responses with less repeated request/error code. |
| Z.12 | Validation command cleanup | Keep existing validation tiers, but make product-readiness validation obvious and non-broad by default. | `scripts/validate_backend.py`, `backend/AGENTS.md`, `docs/QuickStart.md`, `reports/validation/`. | Product readiness has a focused command; broad matrix/runtime validation remains opt-in. |
| Z.13 | Inventory/changelog maintenance | Record Slice Z outcomes only after observable changes are validated. | `SYSTEM_INVENTORY.md`, `CHANGE_LOG.md`. | Inventory and changelog reflect product-readiness improvements without inflating capability claims. |

## Suggested ordering

1. Z.1 startup context cleanup
2. Z.7 product smoke validator
3. Z.2 readiness summary cleanup
4. Z.3 conversation debug and turn artifact contract
5. Z.4 desktop state smoothing
6. Z.5 personality check-up
7. Z.6 desktop backend diagnostics
8. Z.8 QuickStart tightening
9. Z.9 packaging posture clarification
10. Z.10 desktop JS cleanup
11. Z.11 Rust backend bridge cleanup
12. Z.12 validation command cleanup
13. Z.13 inventory/changelog maintenance

This order keeps the backend truth path stable before changing product presentation and documentation.
